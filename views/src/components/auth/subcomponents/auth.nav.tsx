import React, { useState } from 'react';
import '../styles/auth.nav.css'

const NavAuth = () => {
  return (
    <nav className='nav-bar-auth'>
        <section className='nav-items-left-auth'>
            <a href='/' className='spl-logo-auth'>IGNITE</a>
        </section>
    </nav>
  );
}

export default NavAuth;
